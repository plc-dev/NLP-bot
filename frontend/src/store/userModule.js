/* eslint-disable */

export default {
  namespaced: true,

  state: {},
  mutations: {},
  actions: {
    validateSession({ commit }) {
      if (true) {
        //async is true
        commit("setLogin", true);
      }
    }
  }
};
